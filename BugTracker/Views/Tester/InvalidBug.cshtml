@model List<string>

@{
    ViewBag.Title = "InvalidBug";
}

<h2>Invalid Bug!</h2>
<p>The following errors occured while entering bug information:</p>
<ul>
    @foreach (var error in Model)
    {
        <li>@error</li>
    }
</ul>
